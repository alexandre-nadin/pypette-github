#!/usr/bin/env bash
_dir=$(readlink -f $(dirname "${BASH_SOURCE[0]}"))

## Include binaries
export PATH="${PATH}":"${_dir}"
export WORKFLOW_DIR="${HOME}/dev/ctgb"
export CLUSTER_MNT_POINT="${HOME}/dev/ctgb/cluster"
export TMPDIR='/tmp'
"${_dir}/set-dirs"

condactivate ctgb-pipe
ctgb-pipe "$@"
