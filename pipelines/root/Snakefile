import os
import sys
from utils import environ, pipeline
sys.path.insert(0, os.environ['_CPIPE_HOME'])

""" Define the pipeline manager """
pipeman = pipeline.PipelineManager(namespace=globals())

""" Include core modules """
pipeman.includeModule('core/core.sk')

""" Include requested pipeline """
include: pipeman.pipe_snake
